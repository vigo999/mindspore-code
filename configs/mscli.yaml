model:
  provider: openai
  # OpenAI API endpoint - can also use 'base_url' for compatibility
  # Environment variables: MSCLI_ENDPOINT, MSCLI_BASE_URL, or OPENAI_BASE_URL
  endpoint: https://api.openai.com/v1
budget:
  max_tokens: 32768
  max_cost_usd: 10
ui:
  enabled: true
permissions:
  skip_requests: false
  allowed_tools: []
context:
  max_tokens: 24000
  compaction_threshold: 0.85
memory:
  max_items: 200
  max_bytes: 2097152
  ttl_hours: 168
